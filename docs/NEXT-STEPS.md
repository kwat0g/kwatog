# OGAMI ERP — Next Steps After Audit

> Based on Audit Report dated 2026-05-03. Score: 74/100. Defense status: NOT READY.
> This document tells you exactly what to do, in what order, and what prompts to use.

---

## THE REAL PICTURE

The audit score looks worse than reality. Here is why:

| What the audit flagged | What it actually means |
|---|---|
| 36 "PARTIAL" tasks | Code EXISTS and BUILDS. Just not browser-verified. |
| Chain process "NOT CERTIFIED" | Services are present. Just not E2E tested. |
| WebSocket "PARTIAL" | Reverb + Echo + Events exist. Just not tested in 2 tabs. |
| Design system 70% | No hardcoded colors found. Just not visually inspected. |

**The real problems are:**
1. 5 specific defense blockers (must fix before defense)
2. Demo data is too thin (5 employees instead of 50)
3. Automation is reactive — not proactive

**The good news:**
- Security: PASS
- Migrations: 109/109 PASS
- Tests: 73 tests / 177 assertions PASS
- TypeScript build: PASS
- No float money fields: PASS
- No raw IDs exposed: PASS
- No auth tokens in localStorage: PASS

---

## PHASE 1: FIX THE 5 DEFENSE BLOCKERS (do this week)

These are the only things between you and a defensible thesis.
Do them in this exact order.

---

### BLOCKER 1: Run all 3 chains end-to-end in the browser

**Why critical:** Panel will ask you to demonstrate. If the demo breaks live, thesis fails.

**Prompt to use:**

```
Read CLAUDE.md and docs/TASKS.md.

Perform a browser walkthrough of Chain 1 (Order-to-Cash) using demo accounts.
Execute every step in sequence using the actual running application at http://localhost.

Step 1: Login as crm@ogami.test → create a Sales Order for Toyota with 2 product lines
Step 2: Confirm SO → verify MRP auto-triggers → check that draft PRs were created for shortages
Step 3: Login as ppc@ogami.test → review MRP plan → confirm Work Orders → verify material reservations created
Step 4: Login as production@ogami.test → start Work Order → record supervisor output with defect counts
Step 5: Login as qc@ogami.test → create outgoing inspection → record measurements → pass or fail
Step 6: If pass → create Delivery → assign vehicle → confirm as delivered
Step 7: Login as crm@ogami.test → confirm customer receipt → verify draft invoice created
Step 8: Login as finance@ogami.test → finalize invoice → verify GL journal entry posted

At each step:
- Screenshot or describe what you see
- Note any errors or broken UI
- Fix any broken step before proceeding

Report: which steps worked, which broke, what you fixed.
```

Repeat with Chain 2 (Procure-to-Pay) and Chain 3 (Hire-to-Retire).

---

### BLOCKER 2: Generate and visually inspect all PDFs

**Why critical:** Panel will ask for printouts. Broken PDFs = no grade.

**Prompt to use:**

```
Generate every PDF in the system and visually inspect each one.
For each PDF, verify:
- Ogami letterhead (company name, address)
- Correct data populated (not placeholders or null values)
- "Generated by [name] on [date]" footer
- Approval signature lines where required
- CONFIDENTIAL watermark on payslip and financial reports
- Page numbers on multi-page documents

Generate these PDFs using the seeded demo data:
1. Payslip for employee OGM-2026-0001 for the most recent payroll period
2. Purchase Order PO-202604-0001
3. Invoice INV-202604-0001
4. Certificate of Conformance for the most recent delivery
5. 8D Report for the most recent complaint
6. Payroll Register for the most recent period
7. Trial Balance for April 2026
8. Income Statement for April 2026

For any PDF that has null values, broken layout, or missing elements — fix the Blade template now.

Report: which PDFs pass visual inspection, which have issues, what was fixed.
```

---

### BLOCKER 3: Verify WebSocket real-time in two browser tabs

**Why critical:** Real-time production output is your MRP II differentiator. Must work in demo.

**Prompt to use:**

```
Verify that Laravel Reverb WebSocket real-time features work.

First, confirm Reverb is running:
- docker compose ps → is reverb container running?
- If not: docker compose up -d reverb

Then test each real-time feature:

Test 1 — Production output:
- Open Work Order WO-202604-0001 in Tab A (as production manager)
- Open the same Work Order in Tab B (as PPC head)
- In Tab A: record supervisor output (500 good, 12 reject)
- In Tab B: does quantity_produced update without refresh?

Test 2 — Machine breakdown:
- Open Plant Manager dashboard in Tab A
- Set machine IM-003 to breakdown status in Tab B
- In Tab A: does the alert appear in the Alerts panel within 3 seconds?

Test 3 — Notifications:
- Login as depthead@ogami.test in Tab A
- Login as employee@ogami.test in Tab B
- In Tab B: submit a leave request
- In Tab A: does the notification bell badge increment?

If any test fails:
- Check docker compose logs reverb
- Check browser console for WebSocket connection errors
- Fix the Echo channel subscription or broadcast event

Report: which tests passed, which failed, what was fixed.
```

---

### BLOCKER 4: Full dark mode visual pass on every page

**Why critical:** Defense is on a projector. If dark mode breaks on any page, it looks unfinished.

**Prompt to use:**

```
Toggle dark mode on and systematically check every page in the system.
Use the running application at http://localhost.

For each page, check:
- No white/light background visible in dark mode
- No dark text on dark background (contrast failure)
- All colors use CSS variables (already confirmed by grep — just verify visually)
- Status chips readable in both modes
- Modals/dropdowns/tooltips respect dark mode
- Table borders visible but subtle

Pages to check:
Dashboard, Employees list, Employee detail, Employee create form,
Attendance list, Attendance import, Leave list, Leave create,
Payroll periods list, Payroll period detail, Payslip view,
Loans list, Loan create,
COA, Journal Entries, AP Bills, AR Invoices, Financial statements,
Items, Warehouse, Stock levels, GRN,
Purchase Requests, Purchase Orders,
Sales Orders, SO detail, CRM Customers,
Work Orders, Production Gantt, Production output,
QC Inspections, NCR list,
Deliveries, Shipments,
Maintenance, Assets,
Admin: Settings, Roles, Audit Logs

Fix any page that has contrast issues, hardcoded colors, or broken dark mode.

Report: pages that pass, pages that had issues and were fixed.
```

---

### BLOCKER 5: Expand demo data to defense volume

**Why critical:** Audit found only 5 employees, 4 vendors, 5 SOs seeded. A demo with 5 employees looks like a prototype. Your defense needs to look like a real company.

**Prompt to use:**

```
The DemoDataSeeder currently creates insufficient demo data (5 employees, 4 vendors, 5 SOs).
Expand it to match the target volumes in docs/SEEDS.md:

Target volumes:
- 50 employees (mixed: 35 daily-rated factory workers + 15 monthly-salaried office)
  - Distributed across all 11 departments
  - Various employment types (30 regular, 15 probationary, 5 contractual)
  - Realistic Filipino names
  - Each with shift assignments, leave balances, 2 months attendance records

- 4 payroll periods (2 months of semi-monthly payroll)
  - At least 2 finalized periods with GL journal entries
  - Generated payslips for all employees

- 10 vendors with purchase history
- 8 purchase orders (mix of statuses: approved, sent, received)
- 6 sales orders (mix: in production, delivered, invoiced)
- 12 work orders (mix: completed, in progress, planned)
- 18 QC inspections (incoming, in-process, outgoing)
- 3 NCRs (1 critical, 1 major, 1 minor)
- 6 deliveries (mix of delivered and confirmed)
- 10 active employee loans
- 20 leave requests (mix of approved, pending, rejected)
- 12 maintenance records
- 18 assets (machines + molds + vehicles)

Make the data look realistic:
- Use real Filipino names
- Use realistic part numbers and descriptions
- Use realistic peso amounts
- Create logical relationships (SO → WO → QC → Delivery → Invoice)

Run: php artisan migrate:fresh --seed
Verify counts after seeding.
Report: final row counts for each major table.
```

---

## PHASE 2: AUTOMATION ENHANCEMENTS (next 2 weeks)

After blockers are cleared, implement new automation features from `docs/NEW-TASKS.md`.

**Priority order:**

| # | Feature | Why |
|---|---|---|
| 1 | Scheduled MRP auto-run | Currently only triggers on SO. Should also run daily. |
| 2 | Smart alert engine | Proactive warnings before problems happen |
| 3 | Auto payroll period creation | Currently manual. Should auto-create on the 14th and last day. |
| 4 | Delivery status auto-progression | Too many manual clicks between chain steps. |
| 5 | QC auto-fail threshold alerts | Real IATF enforcement, not just data capture. |

See `docs/NEW-TASKS.md` for exact task specs.

---

## PHASE 3: POLISH (week before defense)

```
Read docs/PATTERNS.md and docs/DESIGN-SYSTEM.md.

Polish these specific areas for defense presentation:

1. Plant Manager Dashboard — this is the defense showpiece:
   - All KPI cards must show real data from seeded records
   - StageBreakdown must show active orders at each chain stage
   - Machine utilization table must show live status
   - Defect Pareto must show real defect distribution
   - Alerts panel must show at least 3 real alerts

2. ChainHeader on every chain record:
   - SO detail: all 6 steps, correct active step
   - PO detail: all 8 steps, correct active step
   - WO detail: all 5 steps, correct active step
   - Leave request: all 3 steps (submitted, dept head, HR)
   - Each step must update when the record advances

3. Employee self-service portal:
   - Mobile-responsive layout verified on iPhone/Android viewport
   - Payslip download works (PDF opens, not 404)
   - Leave request form submits correctly
   - DTR shows current month attendance

4. Approval workflows — all 4 levels work:
   - Submit leave as employee → approve as dept head → approve as HR → verified in leave list
   - Submit PR as staff → approve as head → approve as manager → approve as purchasing → verify PO drafted
   - Verify notification appears at each approval level

5. Fix APP_DEBUG=false for defense:
   - Edit api/.env: APP_DEBUG=false
   - Verify API returns generic errors (not stack traces) on 500
   - Verify 422 validation errors still work correctly

Report: list every polish item that was addressed.
```

---

## PHASE 4: DEFENSE PREPARATION

**The 15-minute demo script (use this exactly):**

```
MINUTE 0–1: Login + System Overview
  - Open http://your-vps-domain.com
  - Login as admin@ogami.test
  - Show sidebar: all 12 modules visible
  - Toggle off one module → it disappears from sidebar (feature toggles)
  - Show RBAC: roles + permission matrix

MINUTE 1–3: Hire-to-Retire Chain
  - Login as hr@ogami.test
  - Create new employee (show: auto-generated OGM-2026-XXXX number)
  - Show DTR computation (import CSV → computed columns appear)
  - Process payroll → show DTR feeds payroll calculation
  - Show payslip PDF (CONFIDENTIAL watermark, GL posting)

MINUTE 3–6: Order-to-Cash Chain
  - Login as crm@ogami.test
  - Show existing SO-202604-0003 (pre-seeded) with ChainHeader showing current step
  - Show LinkedRecords: MRP Plan, 3 Work Orders, 1 QC Inspection
  - Navigate to Work Order WO-202604-0007 (in progress)
  - Show production output recording (supervisor entry form)
  - Show Plant Manager Dashboard: live machine status, defect Pareto

MINUTE 6–9: Procure-to-Pay Chain
  - Login as purchasing@ogami.test
  - Show auto-generated PR from MRP shortage (URGENT flag)
  - Show PO with 4-level approval chain visualization
  - Show GRN with incoming QC inspection blocking acceptance
  - Show 3-way match panel (PO vs GRN vs Bill)

MINUTE 9–11: Quality (IATF 16949)
  - Login as qc@ogami.test
  - Show inspection spec for Wiper Bushing WB-001 (dimensions + tolerances)
  - Show outgoing inspection: AQL sample size auto-calculated
  - Show NCR from failed inspection with corrective action
  - Show Certificate of Conformance PDF

MINUTE 11–13: Security Demo
  - Login as employee@ogami.test
  - Try to access /hr/employees → 403 Forbidden
  - Show own payslip in self-service (accessible)
  - Show DevTools → cookies: session is HttpOnly, Secure
  - Show API response: id is 'yR3kLm' not 42

MINUTE 13–15: Panel Q&A Buffer
  - Plant Manager Dashboard on screen
  - Answer questions while showing live data
```

**Likely panel questions and your answers:**

| Question | Your answer |
|---|---|
| "How do you prevent payroll errors?" | 73 automated tests including all 14 holiday combinations. DTR computation engine verified against DOLE formulas. |
| "What makes this different from a generic ERP?" | Three end-to-end chain processes with visible ChainHeader tracking. IATF 16949 at 4 touchpoints. MRP II Gantt with capacity. |
| "How do you secure sensitive employee data?" | HTTP-only cookies (XSS-proof), HashIDs (no enumeration), encrypted SSS/TIN at rest, role-based data masking. |
| "Can this scale to more employees?" | PostgreSQL + Redis caching + background queue jobs. Payroll processes 200 employees as background job. |
| "Is this IATF compliant?" | Incoming QC blocks GRN acceptance. Outgoing QC uses AQL 0.65. Every defect traced. NCR generates corrective actions. CoC auto-generated. |

---

## SUMMARY: WHAT TO DO TODAY

```
Morning: Blocker 1 — Run Chain 3 (Hire-to-Retire) end-to-end in browser
          → Easiest chain. Payroll engine already tested. Just verify UI.

Afternoon: Blocker 5 — Expand DemoDataSeeder to 50 employees
           → Run: php artisan migrate:fresh --seed
           → Verify counts

Tomorrow: Blocker 1 — Run Chain 1 (Order-to-Cash) end-to-end
         Blocker 1 — Run Chain 2 (Procure-to-Pay) end-to-end

Day 3: Blocker 2 — Generate and inspect all PDFs
       Blocker 4 — Dark mode pass on all pages

Day 4: Blocker 3 — Verify WebSocket in two tabs
       Phase 3 — Polish dashboard and ChainHeaders

Day 5: Phase 4 — Practice the 15-minute demo 3 times
       → Record video as backup for defense day
       → Do a dry run on the actual projector

Defense day: Database backup before leaving the house.
```

/** Sprint 8 — Task 69. /maintenance lands on the work-orders list. */
import { Navigate } from 'react-router-dom';
export default function MaintenanceHome() { return <Navigate to="/maintenance/work-orders" replace />; }

# Audit: payroll — 2026-09-06

## Summary

The payroll unit is the most hardened module audited so far: the double-pay guard is genuinely race-proof (DB-unique `payroll_cycle_claims` keyed on a derived cycle key, read-then-insert with the unique index as the real fence), the compute claim uses token-fenced conditional UPDATEs with stale takeover, and void/approve/finalize all lock-then-guard inside transactions. Money math is bcmath string-based end to end; resources keep the decimal-as-string contract; the bank file reconciles its own total against the period before writing. The real defects cluster in the **13th-month path**, which bypasses several guards the normal path enforces: its GL posting drops the BIR correction withholding (PY-01), its net pay is unclamped (PY-03), its payroll_date is unbounded (PY-05), and its tax reconciliation window is status-sensitive (PY-08). Second cluster: gov deductions silently zero when no contribution table exists (PY-02), and the anomaly gate that should backstop that can itself be silently disabled (PY-10) or bypassed via recompute (PY-04). Net-pay clamping writes off unrecovered deductions instead of carrying them forward (PY-06). The SPA is solid (states, string decimals, HashIDs, confirmation dialogs with stakes); findings there are a dead search box and two guard/message mismatches. 40 feature-test files cover the core lifecycle well; the gaps named below are untested.

## Findings

| ID | Category | Severity | Effort | Title | Location | Evidence/Repro |
|---|---|---|---|---|---|---|
| PY-01 | Missing | High | M | 13th-month GL JE posts NET not gross; withheld BIR correction never credited | api/app/Modules/Payroll/Services/PayrollGlPostingService.php:251-256 | Debit thirteenth_expense / credit thirteenth_payable both `$net`; `withholding_tax` (correction delta) has no withholding_payable credit; JE trivially balances so the balance guard never fires |
| PY-02 | Risk | High | M | Missing gov contribution tables silently compute ZERO deductions | api/app/Modules/Payroll/Services/Government/SssComputationService.php:46-55; BirTaxComputationService.php:56-57; PagibigComputationService.php:60; PhilhealthComputationService.php:39-41 | Empty `bracketsEffectiveOn()` result → all four return '0.00' with no error; contrast: de minimis lookup failure hard-blocks (PayrollCalculatorService.php:785-804) |
| PY-03 | Risk | Medium | S | 13th-month net pay unclamped; negative net closes accrual as paid, employee paid nothing | api/app/Modules/Payroll/Services/ThirteenthMonthService.php:307, 437-458; BankFileService.php:156 | `net_pay = amount − correctionDelta` with no zero floor; bank file filters `net_pay > 0` but `markAccrualsPaidOnFinalize` marks paid and cycle claim stands; recovery only via void + re-run |
| PY-04 | Risk | Medium | M | Recompute deletes unresolved anomaly flags and never re-detects — finalize gate bypass | api/app/Modules/Payroll/Controllers/PayrollController.php:89-96; migration 0117_create_payroll_anomaly_flags_table.php:19; detect() only called from ProcessPayrollJob.php:213-221 | flags cascade on payroll_id; recompute replaces the row; no detection on the recompute path (acknowledged at PayrollPeriodService.php:1230-1236) |
| PY-05 | Gap | Medium | S | payroll_date plausibility window skipped on the 13th-month path | api/app/Modules/Payroll/Requests/RunThirteenthMonthRequest.php:20; ThirteenthMonthService.php:154-157 | Only `nullable|date`; no `assertPayrollDateIsPlausible`; payroll_date selects tax rules/brackets/GL date (documented law #5) |
| PY-06 | Gap | Medium | M | Net-pay clamp has no carry-forward adjustment; loans/gov still fully applied | api/app/Modules/Payroll/Services/PayrollCalculatorService.php:330-338, 840-901; PayrollGlPostingService.php:297-305 | Doc says "Excess deductions roll to next period via adjustment" — nothing creates one; loan balance fully decremented; GL credits "Unrecovered Deductions" against expense, writing off the shortfall |
| PY-07 | Bad practice | Medium | S | Gov-deduction decision reads the stored `is_first_half` column, not the derived half | api/app/Modules/Payroll/Services/PayrollCalculatorService.php:241; PayrollAnomalyService.php:88 | cycleKey() docblock says never trust the column; today's create paths set it consistently, but any future writer (seeder/import/migration) moves SSS/PhilHealth/Pag-IBIG/BIR onto the wrong cutoff silently |
| PY-08 | Risk | Medium | S | 13th-month annual tax reconciliation excludes non-finalized periods | api/app/Modules/Payroll/Services/ThirteenthMonthService.php:264-284 | YTD taxable + priorWithheld only sum periods in Finalized/Disbursed; a December run before Nov-H2 is finalized mismeasures correctionDelta (under-withhold; ≥0 clamp prevents refund) |
| PY-09 | Broken process | Medium | S | Period-detail employee search box is a silent no-op | spa/src/pages/payroll/periods/detail.tsx:214 → api/app/Modules/Payroll/Controllers/PayrollController.php:28-80 | SPA sends `search`; PayrollController::index never reads it; 50-row pagination over 200+ employees → operator concludes employee is absent from the run |
| PY-10 | Risk | Medium | M | Known M021: anomaly detection is best-effort; finalize gate reachable with zero flags | api/app/Modules/Payroll/Jobs/ProcessPayrollJob.php:213-221; PayrollPeriodService.php:1220-1243 | detect() inside finally under catch(Throwable)→Log::warning; an invalid `payroll.anomaly.*` setting throws in policy() → no flags; gate reads identical to a clean period (documented in-code, unresolved) |
| PY-11 | Bad practice | Low | S | Adjustments list: unvalidated sort direction → 500 | api/app/Modules/Payroll/Services/PayrollAdjustmentService.php:36-40 | Same bug explicitly fixed with comments in PayrollPeriodService.php:62-66 and PayrollController.php:71, missed here |
| PY-12 | Gap | Low | S | SPA "Run 13th Month" gated on `create OR run`; backend requires `run` | spa/src/pages/payroll/periods/index.tsx:128, 221 vs routes.php:67; RunThirteenthMonthRequest.php:13 | Users with only payroll.periods.create see the button → submit → 403 + generic toast (server message dropped) |
| PY-13 | Bad practice | Low | S | Void audit row records old_values.journal_entry_id after it was nulled | api/app/Modules/Payroll/Services/PayrollPeriodService.php:1436-1447 vs 1475 | AuditLog old_values reads `$locked->journal_entry_id` post-forceFill → always null; original JE id lost from the audit trail |
| PY-14 | Bad practice | Low | S | Void promises "you can recompute" but Voided is locked/non-computable | api/app/Modules/Payroll/Controllers/PayrollPeriodController.php:300; PayrollPeriodService.php:1399-1402; PayrollPeriodStatus::isLocked/isComputable | claimForCompute rejects Voided ("Create a replacement period instead"); docblock references nonexistent `allowedToRecompute` |
| PY-15 | Bad practice | Low | S | Explicit `Content-Type: multipart/form-data` on FormData requests | spa/src/api/payroll/periods.ts:108 | Violates the documented repo convention; benign only because axios 1.18 strips the header for FormData (helpers/resolveConfig.js:79-85); also in attendance/hr/employee-documents APIs |
| PY-16 | Bad practice | Low | S | SCHEMA.md payroll section stale | docs/SCHEMA.md:146-170 | "6 tables" lists 7; missing payroll_cycle_claims, payroll_anomaly_flags, disbursement_proofs, de_minimis_benefits; status enum lacks computed/disbursed/voided; no scope_*/is_thirteenth_month/void/token columns |
| PY-17 | Risk | Low | S | PayrollResource exposes employer shares to every consumer incl. employees | api/app/Modules/Payroll/Resources/PayrollResource.php:49-51 | sss_er/philhealth_er/pagibig_er on all show/list incl. self-service payslip paths; broader than payslip convention needs |
| PY-18 | Risk | Low | M | Statutory/alphalist CSVs lack the formula-injection escape the bank file applies | api/app/Modules/Payroll/Controllers/StatutoryExportController.php:20-29; BirAlphalistService; Exports/Government/SssR3Export.php | BankFileService::escape() documents the hazard ("opened by someone who can move money"); statutory exports carry names/TINs without the guard |
| PY-19 | Gap | Low | S | Pipeline service hides non-canonical-start periods and scoped siblings | api/app/Modules/Payroll/Services/PayrollPeriodService.php:1139-1141 | Slots match only period_start == 1st/16th; `first()` hides the second scoped sibling; legal windows like Aug 3–15 never render; surface currently hidden (route commented) |
| PY-20 | Risk | Low | S | Dated bracket selection ignores is_active; deactivate doesn't remove a table from date-based selection | api/app/Modules/Payroll/Services/GovernmentContributionTableService.php:59-73 | Fallback path filters active(); dated path does not — a deactivated newest table still serves computations |
| PY-21 | Bad practice | Low | S | De-minimis month key inconsistent between methods | api/app/Modules/Payroll/Services/DeMinimisService.php:216-219 vs PayrollCalculatorService.php:788-791 | computeForPayroll keys on period_start month; the calculator keys on payroll_date month (per documented law #5); they disagree when payroll_date crosses the month |

### PY-01 — 13th-month GL posting drops the withheld BIR correction (High, Missing)

`PayrollGlPostingService::postLocked()` builds the 13th-month JE as `debit thirteenth_expense $net; credit thirteenth_payable $net` (PayrollGlPostingService.php:251-256), where `$net = SUM(net_pay) = SUM(gross − correctionDelta)`. When the annual reconciliation withholds tax (`thirteenth_month_correction_delta > 0`, stored as `withholding_tax` on the 13th-month payroll rows — ThirteenthMonthService.php:301-307), that withheld amount belongs to BIR as a withholding-tax liability, but no line credits `withholding_payable`. The entry still balances (debit net = credit net), so the balance assertion at line 308 passes, and the books understate both 13th-month expense and the tax liability by exactly the correction total — the remittance will later leave the bank with no liability on the ledger to clear. The normal branch handles the analogous case correctly (it credits withholding_payable and derives the floor adjustment). Fix: debit expense with gross, credit withholding_payable with `SUM(thirteenth_month_correction_delta)`, credit thirteenth_payable with net. There is **no test coverage** of 13th-month GL posting at all (PayrollGlPostingTest only exercises `is_thirteenth_month => false`), and ThirteenthMonthTest asserts the zero-deduction case ("net_pay must equal gross_pay", ThirteenthMonthTest.php:197-198) while the replacement-run test at lines 150-162 proves correctionDelta can be ₱130,000.

### PY-02 — Missing gov tables silently compute zero deductions (High, Risk)

All four gov computation services treat "no brackets found" as "contribution is ₱0.00": SssComputationService.php:46-55 (empty collection → last() null → 0.00), BirTaxComputationService.php:56-57, PagibigComputationService.php:60, PhilhealthComputationService.php:39-41. `bracketsEffectiveOn()` (GovernmentContributionTableService.php:49-76) falls back from dated rows to the active set, and from there to an empty collection — so a fresh install, a deactivated table, or a payroll_date beyond the newest effective table yields a fully computed, approvable, finalizable period in which **nobody** had SSS/PhilHealth/Pag-IBIG/BIR withheld. The calculator's own comment block (PayrollCalculatorService.php:219-238) treats the contribution basis as load-bearing statutory money, and the de minimis lookup deliberately hard-blocks on failure (lines 785-804) — the asymmetry is the defect. The only backstop is `large_change` anomaly flags versus the prior period, which (a) don't exist for first-payroll employees and (b) depend on a detector that is itself best-effort (PY-10). Fix: make an empty bracket set a BusinessRuleException per employee (error row blocks approve, same as de minimis), or a precompute check that blocks claimForCompute when any agency has no table effective on payroll_date.

## Cross-module flags

- **attendance** — Night-differential basis: `aggregateAttendance()` pays ND as `ndHrs × hourly × 0.10` with **no `day_type_rate` multiplier** (PayrollCalculatorService.php:472-475), while OT on the same rows does multiply by rate (line 469). If the DTR engine records `night_diff_hours` on premium days (holiday/rest-day night shifts), ND is underpaid by the day-rate factor. Verify DTR semantics and either multiply ND by `rate` or document that nd-hours are ordinary-day-only. Also confirm OT min-30min/max-4h and extended-shift auto-OT are enforced DTR-side (not visible from payroll).
- **hr** — CLAUDE.md's payroll law names `Employee::employedDayFraction()` as the proration authority; that method does **not** exist on Employee — proration is a private method of PayrollCalculatorService (PayrollCalculatorService.php:559-587). HR's `FinalPayService::lastSalaryProRated()` consumes `payroll.basic_pay` (correct), but hire/separation proration logic exists in two codebases (payroll calculator vs HR final-pay/clearance paths); a change to one cannot be seen by the other. Consider hoisting the fraction onto Employee as the docs promise.
- **accounting** — PY-01 lands wrong balances on Accounting's books (expense + withholding liability understated for 13th-month runs); the JE reversal path used by void() (JournalEntryService::reverse) was read-only reviewed and looks sound.
- **spa/shared** — PY-15's explicit multipart Content-Type also appears in spa/src/api/attendance/attendances.ts:54, spa/src/api/hr/employees.ts:111, spa/src/api/hr/employee-documents.ts:12 — convention violation is cross-cutting (currently neutralised by axios 1.18; a downgrade or adapter change re-breaks uploads).

## What was NOT checked

- GovernmentTableController CSV import parsing internals, BirAlphalist/statutory numeric accuracy beyond a scan, DisbursementProof MIME edge cases beyond validation rules, PayslipMail blade content, PayrollProgressTracker broadcast channel naming, legacy PostPayrollToGlJob queue-payload behaviour, DeMinimisController store validation detail, SPA adjustments/create.tsx and statutory/index.tsx internals beyond state/number scans.
- Test suite was **not executed** (static review only; every suspicion was confirmable from source, so the one allowed run was not spent).
- Playwright/e2e payroll flows; Reverb broadcast delivery; Docker/nginx headers for the download endpoints.
- Attendance DTR computation (input dependency) and Loan amortization math (dependency) beyond their payroll call sites.

## Code-reading re-audit — 2026-09-14 (56e0d431)

### Method and status

This is a read-only source re-audit of the payroll unit at commit `56e0d431`. I read the required project/audit documents, all in-scope Payroll application files, the payroll SPA pages/API/types, the payroll scheduler entries, and directly relevant Payroll feature tests. Tests, Docker, Artisan, browsers, queues, and database commands were not run. Existing findings are retained by ID below; only unresolved evidence, changed status, and two materially new findings are reported.

### Changed status

| ID | Status | Taxonomy | Severity | Effort | Exact location | Evidence/reproduction |
|---|---|---|---|---|---|---|
| PY-02 | **Mitigated on the normal compute path; residual direct-service risk remains** | Risk | Medium | S | `api/app/Modules/Payroll/Services/PayrollCalculatorService.php:843-871`; `api/tests/Feature/Payroll/MissingGovTableComputeBlockTest.php:74-165` | `computeForEmployee()` now checks every `ContributionAgency` effective on `payroll_date` and throws before writing a row; the focused test covers missing, deactivated, complete, and second-half cases. The four lower-level calculators still return `0.00` for an empty bracket set (`SssComputationService.php:46-55`, `BirTaxComputationService.php:49-57`, `PagibigComputationService.php:46-60`, `PhilhealthComputationService.php:38-41`), so the invariant depends on every writer passing through `PayrollCalculatorService`. |

The two earlier cross-module flags have also materially improved: night-differential pay now applies `day_type_rate` in `api/app/Modules/Payroll/Services/PayrollCalculatorService.php:477-483`, with holiday/rest-day coverage in `api/tests/Feature/Payroll/PayrollCalculatorServiceTest.php:566-590`; hire/separation proration now delegates to one shared helper in `api/app/Modules/Payroll/Support/EmployedDayFraction.php:38-71`, also used by HR final pay at `api/app/Modules/HR/Services/FinalPayService.php:415-439`. The CLAUDE.md wording that names an `Employee` method remains stale, but the implementation no longer has two independent proration formulas.

### Unresolved findings

| ID | Taxonomy | Severity | Effort | Exact file:line | Evidence/reproduction |
|---|---|---|---|---|---|
| PY-01 | Missing | High | M | `api/app/Modules/Payroll/Services/PayrollGlPostingService.php:251-256` | The 13th-month branch debits `thirteenth_expense` and credits `thirteenth_payable` by `$net`, while the BIR correction is stored in `withholding_tax` at `api/app/Modules/Payroll/Services/ThirteenthMonthService.php:301-307`. No withholding-payable credit is created. The JE can balance while understating both expense and BIR liability by the correction amount. `PayrollGlPostingTest.php:86-105` exercises only the regular branch; no 13th-month GL assertion was found. |
| PY-03 | Risk | Medium | S | `api/app/Modules/Payroll/Services/ThirteenthMonthService.php:301-307`; `api/app/Modules/Payroll/Services/BankFileService.php:211-216,235-243` | A correction greater than the 13th-month amount produces a negative `net_pay`; there is no floor or blocking rule. Bank generation filters to `net_pay > 0`, so the row is omitted and a zero-row artifact can reconcile to the filtered expected total. Finalization still marks the linked accrual paid at `PayrollPeriodService.php:1259-1267`, leaving a paid annual run with no employee disbursement. |
| PY-04 | Risk | Medium | M | `api/app/Modules/Payroll/Controllers/PayrollController.php:89-95`; `api/app/Modules/Payroll/Services/PayrollPeriodService.php:1220-1236` | The per-row recompute endpoint replaces a payroll row but never invokes anomaly detection. Finalization only counts persisted unresolved flags, and the service comment explicitly records that single-row recompute has no detection path. A computed period can therefore be recomputed into anomalous values and approved/finalized with no flags. |
| PY-05 | Gap | Medium | S | `api/app/Modules/Payroll/Requests/RunThirteenthMonthRequest.php:16-21`; `api/app/Modules/Payroll/Services/ThirteenthMonthService.php:154-157` | Regular period creation calls the plausibility window, but the 13th-month request accepts any date and `computeAndPay()` parses it directly. A run for year `2025` can be supplied a payroll date in another year, changing the effective annual tax schedule and payment/GL date. |
| PY-06 | Gap | Medium | M | `api/app/Modules/Payroll/Services/PayrollCalculatorService.php:330-343`; `api/app/Modules/Payroll/Services/PayrollGlPostingService.php:291-305` | Negative net is clamped to zero, but no carry-forward `PayrollAdjustment` is created. Loan payments and statutory deductions have already been applied, while the GL posts an “Unrecovered Deductions” salary-expense credit. The documented carry-forward rule therefore remains absent and the shortfall is effectively written off. |
| PY-07 | Bad practice | Medium | S | `api/app/Modules/Payroll/Services/PayrollCalculatorService.php:246`; `api/app/Modules/Payroll/Services/PayrollAnomalyService.php:83-89` | Creation derives the half, but deduction selection and anomaly comparison still read stored `is_first_half`. Any direct factory/import/legacy writer that stores a mismatched flag can move statutory deductions or anomaly baselines to the wrong cutoff. `PayrollPeriod::cycleKey()` correctly derives the half at `api/app/Modules/Payroll/Models/PayrollPeriod.php:331-355`, so the module still has two sources of truth. |
| PY-08 | Risk | Medium | S | `api/app/Modules/Payroll/Services/ThirteenthMonthService.php:264-284` | Annual taxable compensation and prior withholding both include only regular periods with `finalized` or `disbursed` status. A year-end 13th-month run before a regular period is finalized omits that period's compensation/withholding, under-computes the correction, and the non-negative correction clamp prevents a later refund. |
| PY-09 | Broken process | Medium | S | `spa/src/pages/payroll/periods/detail.tsx:210-220`; `api/app/Modules/Payroll/Controllers/PayrollController.php:28-80` | The detail employee search sends `search`, but the payroll index reads no `search` parameter and applies only period, employee, failure, and sort filters. With more than one page of rows, entering an employee name changes the query key but not the result set. |
| PY-10 | Risk | Medium | M | `api/app/Modules/Payroll/Jobs/ProcessPayrollJob.php:213-221`; `api/app/Modules/Payroll/Services/PayrollPeriodService.php:1220-1243` | Anomaly detection remains best-effort under `catch (Throwable)` and only unresolved rows are checked at finalize. Invalid anomaly settings, a detector failure, the 13th-month path, and single-row recompute can all leave zero flags indistinguishable from a clean evaluation. The in-code audit note still documents this exact unresolved gate bypass. |
| PY-11 | Bad practice | Medium | S | `api/app/Modules/Payroll/Services/PayrollAdjustmentService.php:35-40` | Adjustment list sort direction is still passed directly to `orderBy()` after only the sort column is allow-listed. `?direction=sideways` or a non-`asc`/`desc` value can still become a 500, unlike the normalized period/payroll list paths. |
| PY-12 | Gap | Low | S | `spa/src/pages/payroll/periods/index.tsx:127-147`; `api/app/Modules/Payroll/routes.php:67`; `api/app/Modules/Payroll/Requests/RunThirteenthMonthRequest.php:11-14` | The SPA shows “Run 13th Month” to `payroll.thirteenth_month.run OR payroll.periods.create`, but the endpoint authorizes only `payroll.thirteenth_month.run`. A create-only user can see and submit the action, receive 403, and get the generic `Failed to create 13th-month period.` toast. |
| PY-13 | Bad practice | Low | S | `api/app/Modules/Payroll/Services/PayrollPeriodService.php:1436-1476` | `journal_entry_id` is nulled in the same `forceFill()` that marks the period voided, then `old_values.journal_entry_id` reads `$locked->journal_entry_id` after the save. The void audit row therefore records `null` instead of the original JE id, even though the reversal id is retained separately. |
| PY-14 | Bad practice | Low | S | `api/app/Modules/Payroll/Controllers/PayrollPeriodController.php:298-301`; `api/app/Modules/Payroll/Services/PayrollPeriodService.php:1399-1402`; `api/app/Modules/Payroll/Enums/PayrollPeriodStatus.php:38-55` | The void response still promises “you can recompute,” while `Voided` is locked and `isComputable()` permits only Draft/Computed. The service docblock also references nonexistent `allowedToRecompute`; the supported correction path is replacement, not recompute of the voided object. |
| PY-15 | Bad practice | Low | S | `spa/src/api/payroll/periods.ts:105-109` | The FormData upload explicitly sets `Content-Type: multipart/form-data`, contrary to the repository convention that the browser/axios must supply the boundary. It is currently benign only while the installed adapter strips it; an adapter/version change can break disbursement-proof uploads. |
| PY-16 | Bad practice | Low | S | `docs/SCHEMA.md:146-170` | The required schema reference still says Payroll has six tables and omits `payroll_cycle_claims`, `payroll_anomaly_flags`, `disbursement_proofs`, and `de_minimis_benefits`; it also omits current computed/disbursed/voided status and run/scope/fencing fields. This remains documentation drift affecting future audits and migration work. |
| PY-17 | Risk | Low | S | `api/app/Modules/Payroll/Resources/PayrollResource.php:48-60` | `sss_er`, `philhealth_er`, and `pagibig_er` are serialized for every payroll consumer, including employee self-service. The row scope prevents an employee from seeing another employee's row, but the resource still publishes employer-side cost data without a separate field-level policy. |
| PY-18 | Risk | Low | M | `api/app/Modules/Payroll/Services/BirAlphalistService.php:123-139`; `api/app/Modules/Payroll/Services/Statutory/PhilhealthRf1Service.php:63-77`; `api/app/Modules/Payroll/Services/Statutory/PagibigMcrfService.php:63-77`; `api/app/Modules/Payroll/Exports/Government/SssR3Export.php:73-83` | Statutory CSV/XLSX text fields are quoted but do not neutralize formula prefixes (`=`, `+`, `-`, `@`, tab, CR). Names/TINs are exported to files likely opened in spreadsheet software, while the bank-file path has a dedicated formula-injection guard. |
| PY-19 | Gap | Low | S | `api/app/Modules/Payroll/Services/PayrollPeriodService.php:1099-1141`; `api/app/Modules/Payroll/routes.php:54-60` | The retained pipeline service only matches periods whose start is exactly the canonical 1st/16th and uses `first()`, so it hides non-canonical legal windows and any second scoped sibling. The route and SPA are hidden, but the service remains a misleading incomplete operational surface. |
| PY-20 | Risk | Low | S | `api/app/Modules/Payroll/Services/GovernmentContributionTableService.php:59-73,107-115` | Once any dated row exists on/before a date, `bracketsEffectiveOn()` returns all rows for that effective date without `is_active`. Deactivating the newest effective schedule therefore does not remove it from date-based computation, unlike the fallback path. |
| PY-21 | Bad practice | Low | S | `api/app/Modules/Payroll/Services/DeMinimisService.php:216-220`; `api/app/Modules/Payroll/Services/PayrollCalculatorService.php:880-886` | Bulk de-minimis computation keys entries by `period_start` month, while the payroll calculator keys taxable excess by `payroll_date` month. A delayed payroll date crossing a month boundary can use different benefit data in the bulk and employee paths. |

### New findings

| ID | Taxonomy | Severity | Effort | Exact file:line | Evidence/reproduction |
|---|---|---|---|---|---|
| PY-22 | Risk | Medium | M | `api/app/Modules/Payroll/Services/Statutory/Bir1601CService.php:56-67`; `api/app/Modules/Payroll/Services/Statutory/Bir1604CfService.php:44-55`; `api/app/Modules/Payroll/Services/Statutory/PhilhealthRf1Service.php:50-57`; `api/app/Modules/Payroll/Services/Statutory/PagibigMcrfService.php:50-57`; `api/app/Modules/Payroll/Exports/Government/SssR3Export.php:66-82` | Unlike `BirAlphalistService`, these statutory export paths cast decimal database aggregates and components to PHP `float`, add/subtract floats, and return/format the result. Decimal-as-string is the project money contract; aggregate values with cent fractions can be rounded differently from the persisted payroll totals, creating a statutory return that does not reconcile to the source rows. Existing export tests use clean whole/half-peso fixtures and do not exercise adversarial cent values in these paths. |
| PY-23 | Gap | Medium | S | `api/app/Modules/Payroll/Exports/Government/SssR3Export.php:43-52,66-79`; `api/app/Modules/Payroll/Services/PayrollCalculatorService.php:539-547` | The R-3 heading calls column 5 “Total Monthly Compensation,” but the mapper populates it from `basic_pay`; regular payroll basic pay is one half-month amount. A finalized first-half export can therefore report half of the employee's monthly-equivalent compensation under a monthly total heading. The current test only asserts EE/ER shares (`api/tests/Feature/Payroll/StatutoryExportsTest.php:33-53`) and does not assert this field's basis against the official R-3 requirement. |

### Clean areas and verified status changes

- The employee/cycle double-pay fence remains database-backed and derived from dates: `PayrollPeriod::cycleKey()` and `PayrollCalculatorService::claimCycle()` use the unique claim key, with scope/collision coverage in `PayrollPeriodScopeTest.php`.
- Regular payroll-date plausibility is now enforced in the service, not only the request, at `PayrollPeriodService.php:296-318`; the focused scope test covers before-cutoff, far-future, grace-window, and accepted delayed dates.
- Recompute integrity controls are present for loan payments, applied adjustments, and 13th-month accruals at `PayrollCalculatorService.php:135-170`; `PayrollRecomputeIntegrityTest.php:128-245` covers those three money effects.
- 13th-month computation now lands on `Computed`, leaves accruals unpaid until synchronous finalize, and reopens them on void; this is covered by `ThirteenthMonthTest.php:173-208,255-283,338-365`. This does not close PY-01, PY-03, PY-05, PY-08, or PY-10.
- Normal payroll GL posting now handles lateness/undertime and has lock/idempotency, void-link, and fresh re-finalization coverage in `PayrollGlPostingService.php:233-305` and `PayrollMoneyFindingsRegressionTest.php:140-186,233-258`. The separate 13th-month branch remains uncovered and defective (PY-01).
- Bank-file generation now hard-stops on missing bank details, reconciles its included total, and records manual-required failure state at `BankFileService.php:66-90,208-251,313-331`; this does not reject negative 13th-month rows because the query filters them out (PY-03).

### Verification limits

- No PHPUnit/Pest, Artisan, Docker, PostgreSQL, queue worker, scheduler, Reverb, browser, or SPA build command was run, per scope.
- Static reading cannot prove the PostgreSQL locking/fencing behavior, scheduled missed-run recovery, private-file delivery, browser route guards, or deployed restore/recovery behavior.
- Official SSS R-3, BIR, PhilHealth, and Pag-IBIG filing specifications were not available in the requested repository-read scope; PY-23 is reported as a source/heading basis mismatch that still requires confirmation against the official form.
- Existing test files were read for coverage claims only. Their presence is not treated as execution evidence.

No application code, migrations, tests, registry, or roadmap files were changed. The only intended write is this appended section in `audit/payroll.md`.

# Audit: quality — 2026-09-06

## Summary

Quality is the strongest chain-touchpoint subsystem audited so far. All four touchpoints are real and fail closed: incoming QC gates GRN acceptance (`GrnService::assertQcGate` — missing inspections fail closed, archived items cannot waive QC), outgoing QC is the ONLY way to create a delivery (output-bound passed inspection, quantity reserved row-level, legacy inspections refused), every failed inspection auto-opens an NCR inside the completion transaction, and CoC issuance re-verifies stored measurement evidence instead of trusting `status=passed`. Tolerance math is centralized (two identical `evaluate()` implementations, inclusive bounds, no re-implementations outside Quality), the AQL table matches ANSI Z1.4 AQL 0.65 Level II with correct arrow-down collapse to code G, terminal inspection/NCR states are lock-then-guard immutable, and the 8D-escalation lesson is fully applied (`ncr:escalate` exits FAILURE with a separated `failed` bucket; durable `ncr_escalation_deliveries`/`ncr_effectiveness_notifications` idempotency ledgers, migration 0478). All 246 quality-adjacent tests pass (821 assertions, 120s). Findings are edge gaps, not holes: complaint-sourced NCRs structurally never spawn replacement/rework WOs; the single-screen receive-with-QC verdict path is incompatible with tolerance-based quality plans; a spec-less product's outgoing fallback draft is a permanent dead end; in-process QC is advisory-only with no between-operation sampling; and one permission covers the entire inspection lifecycle (no segregation). No path found to ship nonconforming product or issue a false CoC.

## Findings

| ID | Category | Severity | Effort | Title | Location | Evidence/Repro |
|---|---|---|---|---|---|---|
| QC-01 | Gap | Medium | M | Complaint-sourced NCRs can never spawn replacement/rework WOs — the trigger is keyed on `inspection_id` + outgoing stage | api/app/Modules/Quality/Services/NcrService.php:318-357; api/app/Modules/CRM/Services/ComplaintService.php:145-153,284-292 | Complaint NCRs carry `complaint_id` with `inspection_id = null`, so both WO branches' `$locked->inspection_id` guard is false for them. Verified & characterized (see detail) |
| QC-02 | Risk | Medium | M | No segregation of duties across the inspection lifecycle; `qc_inspector` also manages specs, and cancelling an incoming inspection waives the GRN QC gate | api/app/Modules/Quality/routes.php:78-85; api/app/Modules/Inventory/Services/GrnService.php:736-750; api/database/seeders/RolePermissionSeeder.php:714-727 | One permission (`quality.inspections.manage`) covers create+record+complete+cancel; one user can self-pass an outgoing lot that auto-drafts a delivery. Cancelled inspection = "completed decision" → GRN acceptable with zero passing verdicts. Same role holds `quality.specs.manage` (can widen tolerances it is measured against) |
| QC-03 | Stuck process | Medium | M | Single-screen receive-with-QC verdict is incompatible with tolerance-based quality plans — fast-complete throws, whole receipt rolls back | api/app/Modules/Inventory/Services/GrnService.php:1307-1341 (calls InspectionService.php:485-496) | `fastCompleteInspection()` patches blanket `is_pass` with no `measured_value`; `recordMeasurements()` throws "Measurement X requires a result derived from its measured value" for every tolerance row → `receiveWithQc` transaction aborts. Affects exactly the IATF-intended case (plans with dimensional params, e.g. moisture 0–0.2% per IncomingQcTriggerTest:106-107); no test covers this combination |
| QC-04 | Stuck process | Medium | M | Outgoing-QC fallback draft for a spec-less product has no measurement scaffold and can never complete; its presence permanently blocks a spec-backed re-creation | api/app/Modules/Quality/Listeners/TriggerOutgoingQC.php (fallback `Inspection::firstOrCreate`); InspectionService.php:553-555 | Product without active spec → bare Draft with 0 rows → `complete()` throws "no measurement rows"; `recordMeasurements` only patches existing rows. Later adding a spec does not help: the `(stage, entity_type, entity_id, work_order_output_id)` existence guard skips re-trigger. Recovery is manual cancel + manual create |
| QC-05 | Gap | Medium | L | In-process touchpoint is advisory-only: WO completion is not gated on the in-process inspection, and there is no per-operation/output sampling hook | api/app/Modules/Production/Services/WorkOrderService.php:486-548; api/app/Modules/Quality/Listeners/TriggerInProcessQC.php | `complete()` contains no inspection check; exactly one inspection is auto-created at WO start regardless of operation count; `WorkOrderOutputService` has zero inspection references. Failed in-process inspection opens an NCR but does not stop the run; the hard gate exists only at outgoing |
| QC-06 | Risk | Low | S | Delivery confirm requires only *any* POD; CoC auto-attach is best-effort and the attached CoC proof is deletable post-confirm | api/app/Modules/SupplyChain/Services/DeliveryService.php:864-868,902-911; api/tests/Feature/SupplyChain/ZzM044AuditProbeTest.php | CoC generation failure → `Log::warning`, confirm proceeds; test "probe coc can be deleted and replaced after confirmation" passes. Canonical CoC remains in the inspection vault (`CoCService::generateForInspection` is evidence-checked and idempotent), so this is attachment assurance, not certificate integrity |
| QC-07 | Bad practice | Low | S | `measured_value` crosses the API as a JSON float both directions — deviation from decimal-as-string law | api/app/Modules/Quality/Resources/InspectionMeasurementResource.php:28; spa/src/pages/quality/inspections/detail.tsx:139-146 | Resource casts `(float)`; SPA submits `Number(...)`. Known & tracked in-code as M056 (backend validation bounds scale/magnitude, `decimal:0,4`, so rounding exposure is limited to 4dp display fidelity) |
| QC-08 | Gap | Low | S | `docs/SCHEMA.md` QUALITY section is stale and contradicts the enum law | docs/SCHEMA.md:362-380 | Says "5 tables" (~15 exist); invents columns (`inspected_entity_type`, `result pass/fail/pending`, `good_count`); lists NCR source as "incoming/in_process/outgoing/customer" vs actual enum `inspection_fail\|customer_complaint`; omits revisions/plans/templates/calibration/PPAP/shipment-lots/escalation/effectiveness tables |
| QC-09 | Other | Low | L | Defect count is per failing measurement row, not per defective unit | api/app/Modules/Quality/Services/InspectionService.php:508-511,563 | One unit failing 3 parameters counts 3 defects vs Ac — stricter than ANSI Z1.4 (fails more lots than the standard requires); direction is safe (never ships more), but Pareto/Ac semantics overstate defect units |
| QC-10 | Gap | Low | M | Calibration register has no instrument↔inspection linkage; no retroactive validity assessment when an instrument is found out of calibration | api/app/Modules/Quality/Services/CalibrationService.php (whole) | IATF 7.1.5.2 requires assessing prior measurement validity; measurements carry no instrument reference, so an overdue/failed calibration never questions past readings or CoCs |
| QC-11 | Bad practice | Low | S | `Inspection::$fillable` includes `status` against the mass-assignment hardening convention | api/app/Modules/Quality/Models/Inspection.php:48-56 | No live exploit (all writes via service; `CreateInspectionRequest` rules exclude `status` from `validated()`), but CLAUDE.md WIP convention is remove-and-forceFill |

### QC-01 — Complaint-sourced NCRs never spawn replacement/rework WOs (Medium, Gap) — verified & characterized

`NcrService::close()` creates a replacement WO (disposition=scrap) or rework WO (disposition=rework) only when `$locked->inspection_id && $locked->product_id && $locked->affected_quantity > 0` **and** the linked inspection's stage is `Outgoing` (NcrService.php:318-357). CRM's `ComplaintService` creates complaint NCRs with `complaint_id` set and `inspection_id` null (ComplaintService.php:145-153 and the retry path at 284-292), so the guard is structurally false for them — even with product, quantity and a scrap disposition. Net effect: outgoing-QC scrap is automatically replenished, while customer-complaint scrap (defective parts that already reached the customer) relies on manual planning/8D follow-up; the 8D listener (`AutoSpawn8DOnNcrRecurrence`) covers the CRM side but nothing creates production demand. Characterization: this is a design seam, not an accident — the replacement logic is keyed to "re-create lost output of an inspected batch", and complaint NCRs have no batch. Whether Chain 1 requires complaint-driven replacement WOs (linked to the original SO for re-delivery) is a product decision; if yes, the fix is a complaint→SO-line resolution in `close()`, effort M.

### QC-03 — Single-screen receive-with-QC breaks on plan-based GRNs (Medium, Stuck process) — cross-flag Inventory

`GrnService::receiveWithQc()` applies the operator's terminal verdict to every existing incoming inspection via `fastCompleteInspection()`, which patches `['is_pass' => $passed]` on all measurement rows and calls `recordMeasurements()` (GrnService.php:1326-1340). For any row with a tolerance band (every dimensional plan parameter), `recordMeasurements()` enforces "evidence-bearing parameter: no verdict without a reading" and throws (InspectionService.php:485-496). Since `TriggerIncomingQC` creates plan-based inspections synchronously at GRN creation, any GRN whose item has a quality plan with dimensional parameters (the seeded test case is moisture 0–0.2%) cannot be passed *or failed* from the single screen — the whole receipt transaction rolls back with a technical message. The data-safe path (receive as pending_qc, record actual measurements on the inspection page, complete) works and is the more IATF-correct outcome, but the receiving screen's verdict control silently promises something it cannot deliver for the most quality-controlled items. Options: route plan-based GRNs away from the single-screen verdict, or have `fastCompleteInspection` detect tolerance rows and refuse with an operator-facing "record measurements on the inspection" error before touching the transaction.

### QC-04 — Spec-less outgoing fallback draft is a dead end (Medium, Stuck process)

`TriggerOutgoingQC` catches the "no active inspection spec" BusinessRuleException and falls back to `Inspection::firstOrCreate` with **no measurement scaffold** (no `insertScaffoldRows`). That draft can never complete (`complete()` requires ≥1 measurement row; `recordMeasurements` can only patch existing rows), and because the idempotency guard is the row's mere existence, adding the spec later never re-triggers creation for that output batch. The WO's goods then cannot reach a delivery until someone manually cancels the dead draft and manually creates a proper inspection. Recommend: fail the handoff durably (chain-listener `manual_required` outcome, as TriggerIncomingQC does) instead of parking an uncompletable draft, or re-check scaffold presence on replay.

## Cross-module flags

- **Inventory (seam):** QC-03 lives in `GrnService::fastCompleteInspection()` — fix belongs to Inventory with Quality contract preserved. Also: cancelled incoming inspections waive the GRN QC gate by design (F-12, GrnService.php:736-750) — Inventory may want an audit notification when a GRN is accepted with only cancelled (never passed) inspections.
- **Production:** QC-05 — no in-process gate on `WorkOrderService::complete()` and no inspection hook in `WorkOrderOutputService::record()`; between-operation sampling is not modeled at all.
- **CRM:** QC-01 — decide whether complaint-scrap NCRs should create replacement demand; if yes, `NcrService::close()` needs a complaint→SO resolution path.
- **SupplyChain:** QC-06 — CoC attach-on-confirm is best-effort; consider blocking confirm (or a recovery ledger) when the delivery has inspection-linked items but zero `coc` proofs after the attach attempt.
- **Docs:** QC-08 — SCHEMA.md QUALITY section contradicts the enum law and the real schema.

## What was NOT checked

- PPAP service internals (`PpapService.php` 236 lines) and SPC math (`SpcService.php`) beyond confirming routes/permissions and capability thresholds — no time left for numeric verification of Cp/Cpk formulas.
- `ItemQualityPlanService` rollout command internals (test `QualityPlanRolloutCommandTest` passes).
- B2B portal / ReturnManagement quality paths (`supplier_return`/`customer_return` stages) — outside owned scope.
- Live behavior probe of QC-03/QC-04 (read-only audit; characterized by code path + test absence, not executed).
- SPA e2e (Playwright) coverage for quality pages; unit tests (`*.test.tsx`) exist for calibration form and spec editor only.
- WebSocket/real-time aspects and dashboard widget wiring for quality KPIs.

## Code-reading re-audit — 2026-09-14 (56e0d431)

### Re-audit basis

This is a read-only source re-audit at `56e0d431e41d74d422ad81684ff50e0b2b49960e`. The required project, schema, design, seed, roadmap, scope-map, and prior Quality audit documents were read. The owned Quality module, Quality SPA/API/types, directly relevant Quality tests, and the incoming GRN, in-process WO, outgoing delivery, NCR/complaint, production-lot, and traceability consumers were inspected. No tests, Docker, Artisan, database, queue, browser, or live HTTP checks were run.

### Prior findings status

All prior findings remain unresolved in the current source. No status changed from the 2026-09-06 audit, and no in-scope Quality file changed in commit `56e0d431`.

| ID | Current status | Rechecked evidence |
|---|---|---|
| QC-01 | Unresolved | Complaint-created NCRs still have `complaint_id` but no `inspection_id` (`api/app/Modules/CRM/Services/ComplaintService.php:145-153`); replacement/rework creation still requires an outgoing linked inspection (`api/app/Modules/Quality/Services/NcrService.php:319-357`). |
| QC-02 | Unresolved | One permission still covers inspection create, measurement, completion, and cancellation (`api/app/Modules/Quality/routes.php:78-85`); a cancelled incoming inspection remains accepted by the GRN gate (`api/app/Modules/Inventory/Services/GrnService.php:791-805`). |
| QC-03 | Unresolved | The receive-with-QC path still applies a blanket `is_pass` patch to tolerance rows (`api/app/Modules/Inventory/Services/GrnService.php:1110-1119,1382-1396`), while Quality rejects a verdict without a measured value (`api/app/Modules/Quality/Services/InspectionService.php:485-496`). |
| QC-04 | Unresolved | The no-spec outgoing fallback still creates a bare draft with no measurement scaffold (`api/app/Modules/Quality/Listeners/TriggerOutgoingQC.php:142-170`); completion still refuses zero measurement rows (`api/app/Modules/Quality/Services/InspectionService.php:553-555`). |
| QC-05 | Unresolved | WO completion only records the status and emits the outgoing event (`api/app/Modules/Production/Services/WorkOrderService.php:506-565`); output recording has no in-process inspection gate (`api/app/Modules/Production/Services/WorkOrderOutputService.php:208-303`), and the trigger still creates one WO-level inspection (`api/app/Modules/Quality/Listeners/TriggerInProcessQC.php:67-109`). |
| QC-06 | Unresolved | Delivery confirmation requires any proof, while CoC attachment remains best-effort (`api/app/Modules/SupplyChain/Services/DeliveryService.php:928-975`); the existing probe still documents that a CoC can be absent or replaced after confirmation (`api/tests/Feature/SupplyChain/ZzM044AuditProbeTest.php:55-103`). |
| QC-07 | Unresolved | Measurement resources still cast decimal readings to JSON floats (`api/app/Modules/Quality/Resources/InspectionMeasurementResource.php:25-28`), and the SPA still submits `Number(...)` (`spa/src/pages/quality/inspections/detail.tsx:139-149`). |
| QC-08 | Unresolved | The Quality section of `docs/SCHEMA.md` still describes the older five-table schema and obsolete fields/enums (`docs/SCHEMA.md:362-380`). |
| QC-09 | Unresolved | `defect_count` still counts failing measurement rows, not failing sample units (`api/app/Modules/Quality/Services/InspectionService.php:507-516,562-580`). |
| QC-10 | Unresolved | Calibration records still have no instrument reference on measurements or inspection rows, so overdue calibration cannot trigger retrospective evidence assessment (`api/app/Modules/Quality/Services/CalibrationService.php:22-141`; `api/app/Modules/Quality/Models/InspectionMeasurement.php:25-42`). |
| QC-11 | Unresolved | `Inspection::$fillable` still includes `status` (`api/app/Modules/Quality/Models/Inspection.php:48-56`), contrary to the repository's mass-assignment hardening convention. |

### New findings

| ID | Category | Severity | Effort | Finding | Location | Evidence / reproduction |
|---|---|---|---|---|---|---|
| QC-12 | Gap | Medium | S | CoC traceability fields are silently blank for normal outgoing inspections | `api/app/Modules/Quality/Services/CoCService.php:267-279`; `api/app/Modules/Quality/Models/Inspection.php:58-62` | `Inspection::entity_type` is cast to `InspectionEntityType`, but `traceFromInspection()` compares the enum object to the string `'work_order'` with `!==`. The method therefore returns `[null, []]` for output-bound WOs, so a valid CoC omits the WO batch number and material-lot references. `CoCEvidenceIntegrityTest.php:97-109` checks that a PDF is produced but does not assert those payload fields. |
| QC-13 | Broken process | Medium | M | Fractional QC-eligible receipts are either permanently stuck or inspected against a truncated batch | `api/app/Modules/Quality/Listeners/TriggerIncomingQC.php:75-113`; `api/app/Modules/Quality/Services/InspectionService.php:200-218`; `api/app/Modules/Inventory/Services/GrnService.php:895-913` | The normal GRN trigger converts `quantity_received` through `(int) (float)`. A `0.500` raw-material line becomes zero, is skipped, and is then marked `not_required` by the handoff while the GRN gate still considers the line QC-eligible, leaving it pending with no inspection. A `1.500` line becomes a batch of `1`, but later acceptance moves the full `1.500` quantity (`api/app/Modules/Inventory/Services/GrnService.php:554-565`). Existing `GrnQcGateTest::test_fractional_terminal_single_screen_qc_cannot_truncate_and_accept()` covers only the consolidated receive-with-QC route, not ordinary GRN staging or the greater-than-one truncation case. |
| QC-14 | Broken process | Medium | M | A supplier-return NCR can close without its required supplier-return RMA | `api/app/Modules/Quality/Services/NcrService.php:360-376,448-512` | `close()` marks the NCR closed and calls `openSupplierReturnRmaForNcr()`. For an inspection with GRN lineage, that method catches every `Throwable`, logs it, and returns. If Return Management, lineage, accounting, or a dedupe write fails, the NCR is terminal while no RMA/credit/replacement handoff exists and no durable manual-required recovery row is recorded. This is the dead-subsystem failure shape the roadmap's exception standard explicitly rejects. The new return-integration path has no Quality regression proving failure recovery. |
| QC-15 | Risk | High | L | Production traceability records the latest matching material lot, not the lot actually issued to the WO | `api/app/Modules/Production/Services/WorkOrderService.php:295-320,1050-1075`; `api/app/Modules/Quality/Services/TraceabilityService.php:213-263` | At WO start, `captureMaterialLotReferences()` queries each material item and selects `latest('id')` GRN line with a lot, independently of the reservations and stock movements just issued. Receive lot A, then lot B for the same item; reserve/issue lot A and start the WO: the persisted `material_lot_references` points to lot B. Quality trace search then presents the false lot as the backward trace and the real lot has no consuming WO. The inventory issue path records item/location quantity but does not pass lot provenance to this WO snapshot (`WorkOrderService.php:1052-1075`). |
| QC-16 | Gap | Medium | S | Auto-created rework WOs are not exposed in the NCR response or Quality UI | `api/app/Modules/Quality/Services/NcrService.php:76-91`; `api/app/Modules/Quality/Resources/NcrResource.php:61-68`; `spa/src/types/quality.ts:193-220`; `spa/src/pages/quality/ncrs/detail.tsx:179-187` | `close()` can create and persist `rework_work_order_id` (`api/app/Modules/Quality/Services/NcrService.php:339-357`), and the model has a `reworkWorkOrder()` relation, but `show()` eager-loads only `replacementWorkOrder` and `NcrResource` emits only `replacement_work_order`. The detail page consequently cannot link or explain the rework WO; its success toast also checks only `replacement_work_order`. `NcrAutoReworkWoTest.php:74-87` verifies the database row, not the API/resource recovery path. |
| QC-17 | Risk | Low | S | Quality dashboard open-NCR KPI excludes in-progress NCRs | `spa/src/pages/quality/dashboard.tsx:38-41,94-105`; `api/app/Modules/Dashboard/Services/QualityDashboardService.php:42-51` | The SPA requests only `status: 'open'`, but the server-side Quality dashboard defines open NCRs as both `open` and `in_progress`. An NCR that has an action or disposition work in progress disappears from the Quality page's count and list, producing a lower operational queue than the permission-derived dashboard surface. |

### Clean areas and retained controls

The incoming gate still fails closed for missing inspection coverage, archived items, wrong anchors, and QC-eligible lines with no inspection (`api/app/Modules/Inventory/Services/GrnService.php:782-914`). The outgoing delivery path still requires a passed, output-bound inspection and locks the inspection/output/SO lineage before reserving quantity; legacy WO-only rows cannot authorize new deliveries (`api/app/Modules/SupplyChain/Services/DeliveryService.php:453-531`).

Inspection measurement ownership, unresolved-row blocking, terminal lifecycle transitions, automatic NCR creation, and CoC evidence re-reading remain coherent in the normal path (`api/app/Modules/Quality/Services/InspectionService.php:428-612`; `api/app/Modules/Quality/Services/CoCService.php:173-253`). The NCR close race protection, corrective/preventive-action requirement, recurrence ledger, CAPA effectiveness state machine, and idempotent notification ledger remain present (`api/app/Modules/Quality/Services/NcrService.php:225-376`; `api/app/Modules/Quality/Services/EffectivenessService.php:40-125,243-300`).

Resources and frontend payloads use HashID strings for entity identifiers in the inspected Quality surfaces, and Quality routes have server-side feature and permission middleware (`api/app/Modules/Quality/routes.php:23-183`; `api/app/Modules/Quality/Resources/InspectionResource.php:17-124`; `api/app/Modules/Quality/Resources/NcrResource.php:17-79`). The prior traceability JSON-array containment defect is fixed and covered by `api/tests/Feature/Quality/TraceabilityPpapAuditRegressionTest.php:125-161`. The existing Quality pages generally provide loading, error, empty, data, and placeholder-data states; no additional page-state defect was promoted from static reading.

### Verification limits

No tests, Docker, Artisan, PostgreSQL, queue worker, scheduler, browser, SPA build, PHPStan, or live HTTP request was run, per scope. Findings QC-12 through QC-15 and QC-16 are source-path reproductions and should receive focused regression tests before status is changed. Existing tests were read for coverage context only; historical pass counts in the prior audit were not re-verified. PPAP/SPC numeric behavior, WebSocket delivery, dashboard cache freshness, external file/provider behavior, and deployed migration/restore behavior remain unproven here.

### No code change

No application code, migration, test, registry, or roadmap file was modified. This append to `audit/quality.md` is the only change made for this re-audit.

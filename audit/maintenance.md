# Audit: maintenance — 2026-09-06

## Summary

The Maintenance module is internally disciplined: a single authoritative state machine, lock-then-recheck idempotency on every WO-creation path (per-schedule, per-machine predictive), race-tested completion/recompute/cost accumulation, careful polymorphic HashID decoding, and a durable outbox handoff for the daily preventive sweep. Its 30-test filter passes clean (95 assertions, ~28s). The defects are concentrated at the **completion seams** — exactly where this audit was pointed. MRP-02 is confirmed from this side: MWO completion resets a mold's shot count but never flips its status back to `available`, stranding it in `maintenance` forever. Worse, the same class of break hits machines: MWO start/complete/cancel write `machines.status` with raw `forceFill` and never dispatch `MachineStatusChanged`, so Production's restoration listener never closes open `machine_downtimes` rows, the live dashboard never sees maintenance transitions, and the reserved `machine_downtimes.maintenance_order_id` link was never wired by anyone — MWO downtime data is invisible to OEE and to the module's own downtime analytics. Secondary: asset-linked maintenance is entirely absent (`assets.under_maintenance` is written by nobody), predictive-WO dedup is broken by a case-sensitive LIKE (dormant since the condition-reading UI was scope-cut), molds under corrective MWO stay schedulable, and in-progress MWOs cannot be reassigned.

## Findings

| ID | Category | Severity | Effort | Title | Location | Evidence/Repro |
|---|---|---|---|---|---|---|
| MT-01 | Broken process | High | S | Mold never restored to `available` when its MWO completes (this side of MRP-02) | api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:239-264 | complete() mold branch resets shots + bumps counters but never writes `status`; MoldService.php:121 is the only writer and only sets `maintenance` |
| MT-02 | Stuck process | High | M | MWO machine-status writes bypass `MachineStatusChanged`; breakdown downtime rows never closed on MWO-driven restoration | api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:195-200, 266-271, 294-299 | forceFill('status') with no event; HandleMachineBreakdown::handleRestoration (Production) only runs on MachineStatusChanged, dispatched solely by MRP MachineService.php:109 |
| MT-03 | Gap | High | M | MWO↔downtime seam dead in both directions: `maintenance_order_id` never written, no `planned_maintenance` rows ever created, MWO `downtime_minutes` never reach OEE | api/database/migrations/0084_create_machine_downtimes_table.php:12,27; 0101_…:14; api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:233 | Migration comment "reserved for Sprint 8 Task 69" — Sprint 8 never wrote it; OeeService.php:25,49 reads only machine_downtimes; MWO field is write-only (grep: no consumer) |
| MT-04 | Risk | Medium | M | No one-open-MWO-per-machine guard; cancel/complete restore machine on status alone, not ownership | api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:294-299 | Preventive + corrective MWOs coexist on one machine; cancelling the not-started MWO-B flips the machine MWO-A is actively working back to `idle` |
| MT-05 | Broken process | Medium | S | Predictive corrective-WO dedup never matches: case-sensitive `LIKE '%predictive%'` vs description `[Predictive] …` | api/app/Modules/Maintenance/Services/PredictiveMaintenanceService.php:236-243, 254-261 | PG LIKE is case-sensitive; both the pre-check and the under-lock re-check miss every existing WO → duplicate corrective MWOs when the surface re-enables (currently dormant: reading routes scope-cut 2026-08-08, but cron still calls evaluateAllMachines) |
| MT-06 | Gap | Medium | M | Asset-linked maintenance absent: MaintainableType has no asset case; asset `under_maintenance` written by nobody | api/app/Modules/Maintenance/Enums/MaintainableType.php:12-13; api/app/Modules/Dashboard/Services/Analytics/AssetWidgetAnalytics.php:31 | Grep: `UnderMaintenance` appears only in reads (dashboard widget, permanently 0); ZzM045FleetProbeTest prints `MaintainableType cases=["machine","mold"]` |
| MT-07 | Risk | Medium | S | MWO start() on a mold never marks the mold `maintenance`; corrective MWO leaves it schedulable for production | api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:201-213 | Mold branch only writes MoldHistory; asymmetric with machine branch (line 195-200) |
| MT-08 | Gap | Medium | M | No reassignment of in-progress MWOs | api/app/Modules/Maintenance/Support/MaintenanceWorkOrderStateMachine.php:15-21 | TRANSITIONS has no `in_progress → assigned`; shift handoff requires cancel + recreate (which also re-opens the schedule sweep) |
| MT-09 | Bad practice | Low | S | Resource exposes actions the caller cannot perform | api/app/Modules/Maintenance/Resources/MaintenanceWorkOrderResource.php:92-106 | `start`/`cancel` listed for any `maintenance.view` user; buttons render then 403 (route needs `maintenance.wo.complete`) |
| MT-10 | Bad practice | Low | S | Per-row `Machine::find`/`Mold::find` in resource | api/app/Modules/Maintenance/Resources/MaintenanceWorkOrderResource.php:26-28 | 100-row list page → 100+ extra queries per load |
| MT-11 | Gap | Low | M | Spare-part issue irreversible | api/app/Modules/Maintenance/Services/SparePartUsageService.php:33-94 | No void/return endpoint; wrong qty/location permanently consumes stock; cancelling an MWO after issue returns nothing |
| MT-12 | Other | Low | S | Doc/schema drift + stale-hours baseline at completion | docs/SCHEMA.md:384-396; api/app/Modules/Maintenance/Services/MaintenanceScheduleService.php:177-180 | SCHEMA.md omits mwo_number, running_hours_baseline, stock_movement_id, machine_condition_readings; recompute baseline = running_hours_total last refreshed 01:30 (bounded ~1-day under-baseline) |
| MT-13 | Risk | Low | S | Condition readings accept implausible values; duplicate timestamps allowed | api/app/Modules/Maintenance/Controllers/MachineConditionReadingController.php:96-99 | `value => ['required','numeric']` only — no per-metric plausibility bounds, no recorded_at uniqueness (dormant surface; matters if re-enabled) |

### MT-01 — Mold never restored to `available` after MWO completion (High, Broken process)

Confirmed from the owning side of the seam. `MoldService::incrementShots()` flips a mold to `MoldStatus::Maintenance` the moment `current_shot_count >= max_shots_before_maintenance` (api/app/Modules/MRP/Services/MoldService.php:121) and nothing in MRP ever flips it back (only create/retire write status otherwise). `MaintenanceWorkOrderService::complete()` owns the mold completion branch — it locks the mold, resets `current_shot_count` to 0, stamps `last_maintenance_at`, bumps `maintenance_count`, accumulates `total_maintenance_cost` and writes `MoldHistory::MaintenanceCompleted` (lines 239-264) — but never touches `status`. Result: every mold that reaches its shot ceiling is permanently `maintenance` after its first PM cycle, invisible to `scopeAvailable()` and to capacity planning. **Where the fix lives: here, in `complete()`** — it already holds the lock and owns the reset; MRP has no MWO-completion event to listen for (none exists, see MT-02/MT-03 pattern). Restore to `available` unless the mold is `retired`/`decommissioned`. `MaintenanceDoubleCompleteRaceTest` creates molds with status `in_use` and never asserts post-completion status, which is why this shipped green. The MRP-12 note (dead `resetShotCount()`/`recordMaintenance()`) is the same seam: this branch should ideally call those instead of re-implementing inline, but the status flip is missing from both copies.

### MT-02 — MWO machine-status writes bypass `MachineStatusChanged` (High, Stuck process)

`start()`, `complete()` and `cancel()` each write `machines.status` via `forceFill([...])->save()`. `MachineStatusChanged` is dispatched in exactly one place in the codebase — `MachineService::updateStatus()` (MRP, MachineService.php:109) — and there is no model observer, so maintenance-driven transitions are silent. Three concrete consequences: (1) Production's `HandleMachineBreakdown::handleRestoration` closes open `machine_downtimes` rows only on a `breakdown|maintenance → idle|running` **event**; a machine that entered breakdown (downtime row opened by WO pause), got repaired under an MWO and was set back to `idle` by `complete()` leaves its downtime row open forever — MTTR/availability math then clips an ever-growing interval into every reporting window (DowntimeAnalyticsService.php:283 treats NULL end_time as "now"). (2) The `MachineStatusChanged` broadcast (`ShouldBroadcast`) that drives the live machine board never fires for maintenance transitions — floor dashboards show stale states. (3) Starting an MWO on a `running` machine silently flips it to `maintenance` without the pause side-effects production expects. Fix direction: route MWO status changes through `MachineService::updateStatus()` (or dispatch the event with correct from/to inside the same transaction), not raw forceFill. Cross-module coordination with MRP + Production required.

### MT-03 — MWO↔downtime data seam dead in both directions (High, Gap)

The schema reserved the link and the code documents it, but nobody implemented it: migration 0084 created `machine_downtimes.maintenance_order_id` "reserved for Sprint 8 Task 69" (no FK, never written — grep across api/app finds it only in the MachineDowntime `$fillable`), and migration 0101's header claims corrective MWOs are "auto-linked from machine_downtimes.maintenance_order_id when present". Sprint 8 shipped neither direction: no `planned_maintenance`-category downtime row is ever created by anyone (grep: only the enum and OeeService's plan-time classification read it), so MWO time on a machine is invisible to OEE availability/performance — yet the mobile completion sheet tells the tech the minutes are "booked against OEE" (spa/src/pages/maintenance/mobile/work-order.tsx:370). The `downtime_minutes` captured at completion is write-only: returned by the resource and stored on the row, but consumed by no analytics, no OEE calc, no report (grep confirms). Meanwhile the module's own DowntimeAnalyticsService reads exclusively from `machine_downtimes`, so the maintenance dashboard measures downtime from a table maintenance never writes. Fix: at `start()` open a `planned_maintenance` downtime row tagged with the MWO id; at `complete()`/`cancel()` close it (or reconcile against the tech-entered minutes).

### MT-05 — Predictive dedup defeated by case-sensitive LIKE (Medium, Broken process)

`hasOpenCorrectiveWoForMachine()` filters `description LIKE '%predictive%'` and the under-lock re-check in `createCorrectiveWorkOrder()` repeats the same pattern — but the description written is `'[Predictive] %s — Auto-generated from condition monitoring.'` with a capital P. PostgreSQL `LIKE` is case-sensitive, so neither check ever matches an existing row: the one-open-corrective-WO-per-machine guarantee is void, and two breach windows (or a manual reading racing the 02:00 sweep) produce duplicate MWOs. Currently latent — the condition-reading entry routes were scope-cut 2026-08-08 (routes.php:58-84) — but `evaluateAllMachines()` still runs nightly from the preventive job, and the re-enable checklist in routes.php restores exactly this path. Fix is one-word (`ilike`, or match on a dedicated column/type instead of prose — string-matching descriptions is itself the bad practice here); a dedup regression test should accompany it since MobileMaintenanceTest only covers the single-trigger case.

## Cross-module flags

- **mrp (MRP-02)** — confirmed from both sides. The restore belongs in `MaintenanceWorkOrderService::complete()` (MT-01); MRP's `MoldService` also has no restore path, and its dead `resetShotCount()`/`recordMaintenance()` (MRP-12) are the same seam. Fix once, here.
- **mrp** — `MachineService::updateStatus()` is the sole `MachineStatusChanged` emitter; MT-02's fix should route through it (or MRP should expose a transition primitive maintenance can call inside its transaction).
- **production** — `machine_downtimes.maintenance_order_id` is never written by any module (MT-03); Production's downtime-row lifecycle (`HandleMachineBreakdown`) silently depends on an event Maintenance never emits (MT-02). Joint fix.
- **assets** — `AssetStatus::UnderMaintenance` is read by two dashboard widgets and written by nobody; assets can link machines/molds (`linked_type/linked_id`, `molds.asset_id`) but MWO start/complete never syncs asset status (MT-06). Either extend `MaintainableType` or add an asset-status hook keyed off the machine/mold asset link.
- **inventory** — spare-part seam is healthy from this side: issuance delegates to `StockMovementService::move()` (WAC snapshot, lock ordering, spare_part type gate); no negative-stock exposure introduced by Maintenance. MT-11 (no reversal path) is a Maintenance-side gap, not an Inventory defect.

## What was NOT checked

- Inventory `StockMovementService` internals (trusted as the shared guard; only its call site here was reviewed).
- MRP `CapacityPlanningService` consumption of mold/machine status beyond confirming the `available` scope exists.
- WebSocket/outbox delivery mechanics of `MaintenanceWorkOrderCreated` (durable handoff tests pass; delivery path not traced).
- SPA runtime behaviour — pages reviewed statically only (no browser run); downtime/schedules/create pages spot-checked for states, not exercised.
- OeeService internals beyond confirming its downtime-category inputs.
- Settings seed values beyond existence (`0415`, `0342`, `0347`, `0284`, `0384`, `0442` all present).
- `MachineConditionReadingController` index/trend/healthSnapshot paths in depth (surface hidden; only the store-path validation reviewed).

## Code-reading re-audit — 2026-09-14 (56e0d431)

### Re-audit verdict

The current source includes effective fixes for the three highest-impact completion-seam findings from the 2026-09-06 audit: mold completion now restores a non-retired mold to `available`; machine transitions use `MachineService` and therefore emit the durable `MachineStatusChanged` path; and machine MWOs open and close a `planned_maintenance` downtime row linked by `maintenance_order_id`. The focused tests read for those fixes cover the intended happy paths and the restoration listener/OEE handoff.

The remaining material risk is authorization and lifecycle ownership rather than the previously dead machine/downtime seam. Maintenance work-order list/show/action code has no employee row scope, while the seeded maintenance technician role has execute permission. A technician can therefore operate any work order whose hash ID they can obtain, not only assigned or available work. Completion also presents two competing downtime facts: the entered value is stored on the MWO, but OEE consumes elapsed duration from the downtime ledger, so the value confirmed to the technician is not necessarily the value reported to OEE.

### Prior finding status

| ID | Current status | Current evidence |
|---|---|---|
| MT-01 | Resolved in current source | `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:258-279` restores non-retired molds to `MoldStatus::Available`; `api/tests/Feature/Maintenance/MoldStatusRestoreOnCompleteTest.php:63-89` asserts available and retired behavior. |
| MT-02 | Resolved in current source | `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:202-217,291-301` uses `MachineService::transitionStatus`; `api/tests/Feature/Maintenance/MaintenanceWorkOrderMachineDowntimeTest.php:141-188` covers emitted transitions and restoration closure. |
| MT-03 | Resolved for the original dead-seam evidence; residual semantics remain in MT-15 | `maintenance_order_id` is written and the planned row is closed at `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:211-217,367-382`; OEE consumption is asserted at `api/tests/Feature/Maintenance/MaintenanceWorkOrderMachineDowntimeTest.php:192-209`. |
| MT-04 | Unresolved | No one-open-MWO ownership guard remains around machine start/restore; see current finding table. |
| MT-05 | Unresolved but intentionally latent | Both predictive dedup queries still use case-sensitive `LIKE` at `api/app/Modules/Maintenance/Services/PredictiveMaintenanceService.php:234-243,254-261`; the HTTP reading surface remains scope-cut, but the scheduled job still calls predictive evaluation at `api/app/Modules/Maintenance/Jobs/GeneratePreventiveMaintenanceJob.php:88-91`. |
| MT-06 | Unresolved | `MaintainableType` still has only machine and mold at `api/app/Modules/Maintenance/Enums/MaintainableType.php:8-11`; no maintenance path updates linked asset status. |
| MT-07 | Unresolved | Mold start records history only at `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:220-231`; it does not make a mold unavailable while corrective maintenance is in progress. |
| MT-08 | Unresolved | `in_progress` has only `completed` and `cancelled` transitions at `api/app/Modules/Maintenance/Support/MaintenanceWorkOrderStateMachine.php:15-21`; no handoff/reassignment transition exists. |
| MT-09 | Unresolved, with a partial SPA mitigation | The resource still advertises `start`/`cancel` to any viewer and `complete`/`cancel` to any viewer in `api/app/Modules/Maintenance/Resources/MaintenanceWorkOrderResource.php:92-105`; desktop buttons add a client permission check, but mobile relies on `available_actions` and has no equivalent permission check. |
| MT-10 | Unresolved | Each work-order and schedule resource still performs a per-row target lookup at `api/app/Modules/Maintenance/Resources/MaintenanceWorkOrderResource.php:26-28` and `api/app/Modules/Maintenance/Resources/MaintenanceScheduleResource.php:21-23`. |
| MT-11 | Unresolved | The only spare-part route is an irreversible create/issue operation at `api/app/Modules/Maintenance/routes.php:55-56`; no return, void, or cancellation reconciliation exists. |
| MT-12 | Unresolved | `docs/SCHEMA.md:384-396` still omits fields added by the maintenance migrations, and machine-hour completion still snapshots the persisted daily runtime at `api/app/Modules/Maintenance/Services/MaintenanceScheduleService.php:175-180`. |
| MT-13 | Deferred by scope cut, not an active reachable-page defect | Condition-reading routes are explicitly hidden at `api/app/Modules/Maintenance/routes.php:58-84`; the retained service code still has the old validation limits, but there is no supported HTTP/UI path. |

### Current findings

| ID | Category | Severity | Effort | Location | Evidence / reproduction |
|---|---|---|---|---|---|
| MT-04 | Risk | Medium | M | `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:202-218,291-301` | Start MWO-A on a machine, then start MWO-B for the same machine. MWO-B sees the machine already in `maintenance`, so it opens no distinct downtime row. Completing or cancelling MWO-B then calls `transitionStatus(...Idle...)` based only on the machine status and can restore the machine while MWO-A remains `in_progress`. There is no machine ownership or one-open-MWO invariant. |
| MT-05 | Broken process | Medium | S | `api/app/Modules/Maintenance/Services/PredictiveMaintenanceService.php:234-243,254-261` | Existing generated descriptions contain `[Predictive]` with a capital `P` at lines 271-274, while both dedup checks search `LIKE '%predictive%'`. PostgreSQL `LIKE` is case-sensitive, so a second breach window can create another corrective WO for the same machine. The current hidden reading route reduces user reachability but does not remove the scheduled evaluator. |
| MT-06 | Gap | Medium | M | `api/app/Modules/Maintenance/Enums/MaintainableType.php:8-11`; `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:202-231,258-301` | Maintenance supports only machine/mold targets and never resolves a linked asset or writes `AssetStatus::UnderMaintenance`. A machine or mold linked to an asset can complete maintenance while the asset dashboard's maintenance state remains unchanged. |
| MT-07 | Risk | Medium | S | `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:220-231` | Starting a mold corrective MWO writes `MoldHistory` but not `molds.status`. A mold that is still `in_use` remains eligible to be scheduled/assigned while its maintenance order is in progress; only completion later writes `available`. |
| MT-08 | Gap | Medium | M | `api/app/Modules/Maintenance/Support/MaintenanceWorkOrderStateMachine.php:15-21`; `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:167-185` | The assign service must transition the order to `assigned`, but an already `in_progress` order cannot transition back to `assigned` and there is no replacement assignment operation. A technician handoff requires cancel/recreate, losing the same-order continuity and reopening preventive-generation eligibility. |
| MT-09 | Bad practice | Low | S | `api/app/Modules/Maintenance/Resources/MaintenanceWorkOrderResource.php:92-105`; `spa/src/pages/maintenance/mobile/work-order.tsx:163-222` | A viewer receives `available_actions: ['start','cancel']` or `['complete','cancel']` regardless of permission. The mobile detail page renders `Start` from that array without calling `usePermission`; a `maintenance.view` user can see an actionable control that predictably returns 403. Desktop permission checks are only UX mitigation, not a resource contract. |
| MT-10 | Bad practice | Low | S | `api/app/Modules/Maintenance/Resources/MaintenanceWorkOrderResource.php:26-28`; `api/app/Modules/Maintenance/Resources/MaintenanceScheduleResource.php:21-23` | A 100-row work-order or schedule response executes a target `Machine::find`/`Mold::find` for every row even though list services eager-load other relationships. This creates avoidable N+1 queries on the primary maintenance lists. |
| MT-11 | Gap | Low | M | `api/app/Modules/Maintenance/Services/SparePartUsageService.php:61-90`; `api/app/Modules/Maintenance/routes.php:55-56` | Issuing a part posts an inventory movement and an immutable usage row. If the wrong item, quantity, or location is selected, or the MWO is later cancelled, no maintenance-side reverse/return path exists; the stock movement and cost remain committed. |
| MT-12 | Bad practice | Low | M | `docs/SCHEMA.md:384-396`; `api/app/Modules/Maintenance/Services/MaintenanceScheduleService.php:175-180` | The documented maintenance schema does not list `mwo_number`, `running_hours_baseline`, `stock_movement_id`, or `machine_condition_readings` details. Separately, completing a machine-hour MWO recalculates its baseline from `machines.running_hours_total`, which is refreshed by the scheduled recompute rather than at completion, so a completion can leave the next threshold up to the last runtime refresh behind. |
| MT-14 | Risk | High | M | `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:52-95,167-185,188-237,243-345`; `api/app/Modules/Maintenance/Controllers/MaintenanceWorkOrderController.php:33-36,85-87`; `api/database/seeders/RolePermissionSeeder.php:836-845` | The list/show/action services accept a work-order model and actor but never apply an employee/department/assignment scope. The seeded `maintenance_tech` has `maintenance.view`, `maintenance.wo.create`, and `maintenance.wo.complete` but not assignment permission. A maintenance technician can request another employee's MWO by hash ID and start, complete, or cancel it because the route checks only the broad execute permission. The Action Center has the same all-open query at `api/app/Modules/Dashboard/Services/ActionCenterService.php:284-306` and task gate at `api/app/Modules/Dashboard/Services/ActionCenterTaskService.php:169-170`; flag that aggregator seam for unit 9 consolidation. |
| MT-15 | Broken process | Medium | M | `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:251-256,367-382`; `spa/src/pages/maintenance/mobile/work-order.tsx:299-370`; `api/app/Modules/Production/Services/OeeService.php:47-54` | Completion stores the technician's `downtime_minutes` on the MWO, but `closeMachineDowntime()` sets the ledger duration from `start_time` to completion and OEE sums that ledger duration. The existing focused test deliberately demonstrates the split: entering 45 minutes after a 40-minute ledger interval leaves MWO downtime at 45 and ledger duration at 40 (`api/tests/Feature/Maintenance/MaintenanceWorkOrderMachineDowntimeTest.php:63-80`). The mobile confirmation says the entered number is booked against OEE (`mobile/work-order.tsx:364-370`), which is not true. |
| MT-16 | Gap | Medium | S | `api/app/Modules/Maintenance/Controllers/MaintenanceWorkOrderController.php:116-123`; `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:318-343` | Cancel accepts an unvalidated `reason` from `Request`, including null or an empty string, and the service records only `Cancelled.` when no reason is supplied. The UI's `ReasonDialog` has a five-character minimum, but the server does not enforce the irreversible-action audit requirement. Direct API callers can cancel without an accountable explanation. |
| MT-17 | Broken process | Medium | S | `spa/src/pages/maintenance/downtime/index.tsx:85-104,214-218,254-315` | Only the summary query has an error state and retry. Trend, top-machine, and all-machine query failures fall through to normal-looking empty states such as `No downtime data`, `All machines are operating normally`, or `No machines found`, with no retry. A partial API outage can therefore be presented as healthy maintenance data. |

### Clean areas and intentional non-findings

- MWO creation from a schedule locks and rechecks the schedule before creating a duplicate, at `api/app/Modules/Maintenance/Services/MaintenanceWorkOrderService.php:99-119`.
- Completion and schedule recomputation lock authoritative rows and reject stale lifecycle transitions, with focused coverage in `MaintenanceDoubleCompleteRaceTest.php` and `MaintenanceScheduleRecomputeRaceTest.php`.
- HashID decoding is present for polymorphic target inputs and assignment/part inputs; maintenance resources return hashed IDs and decimal values as strings.
- Preventive generation is staged through the durable outbox and the current scheduler runs the runtime recompute before the preventive sweep (`api/routes/console.php:20-24,111-116`; `api/app/Modules/Maintenance/Jobs/GeneratePreventiveMaintenanceJob.php:68-85`).
- The desktop and mobile maintenance routes are lazy-loaded and module/permission guarded. The condition-reading and machine-health surfaces are intentionally hidden by the documented no-IoT scope cut, so their unreachable pages and clients are not counted as reachable-route breakage in this re-audit.
- The current commit `56e0d431` changes MRP auto-PR rerun handling, not the maintenance module; the MT-01/02/03 status changes are present in the current tree from earlier maintenance fixes.

### Verification limits

This was a read-only code-reading pass. No tests, Docker commands, Artisan commands, browser runs, migrations, queue workers, or live HTTP requests were run. The focused maintenance tests and directly relevant MRP, Production, Inventory, Dashboard, seed, route, and migration files were read for evidence only. Runtime behavior of nested transactions, queued outbox/listener delivery, database query counts, permission resolution, and browser rendering remains unverified. The Action Center observations are cross-module flags for consolidation, not a claim that its shared implementation was fully re-audited here.

### No code change

No application code, migrations, tests, registry, or roadmap content was modified. This section was appended to `audit/maintenance.md` only.
